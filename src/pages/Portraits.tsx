import React from "react";
import PhotoGrid from "../components/PhotoGrid";

const Portraits = () => {
	return (
		<section>
			<PhotoGrid />
		</section>
	);
};

export default Portraits;

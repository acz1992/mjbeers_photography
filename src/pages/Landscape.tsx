import React from "react";
import PhotoGrid from "../components/PhotoGrid";

const Landscape = () => {
	return (
		<section>
			<PhotoGrid />
		</section>
	);
};

export default Landscape;

{
	"version": 2,
	"builds": [
		{
			"src": "frontend",
			"use": "@vercel/static-build",
			"config": { "distDir": "frontend/build" }
		},
		{
			"src": "backend_sanity",
			"use": "@vercel/node"
		}
	],
	"routes": [
		{
			"src": "/(.*)",
			"dest": "/frontend/$1"
		}
	]
}

{
	"version": 2,
	"builds": [
		{ "src": "backend_sanity/**", "use": "@vercel/node" },
		{ "src": "frontend/**", "use": "@vercel/static-build" }
	],
	"routes": [
		{ "src": "/api/.*", "dest": "backend_sanity/$0" },
		{ "src": "/(.*)", "dest": "frontend/$1" }
	]
}

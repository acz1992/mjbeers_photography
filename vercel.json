{
	"version": 2,
	"builds": [
		{
			"src": "frontend",
			"use": "@vercel/static-build",
			"config": { "distDir": "dist" }
		},
		{
			"src": "backend_sanity",
			"use": "@vercel/node"
		}
	],
	"rewrites": [{ "source": "/(.*)", "destination": "/frontend/$1" }]
}
